# AI Command Fixer 内容 Backlog

日期：2026-06-11

## 1. 博客选题

### P0 发布前必须准备

| 优先级 | 中文标题 | English title | 目标 |
|---|---|---|---|
| P0 | 为什么 AI 生成的命令复制到终端会失败？ | Why AI-generated commands break when copied into terminals | 教育核心问题，解释换行、缩进、引号、SQL 折行 |
| P0 | 为什么不能简单把换行替换成空格？ | Why replacing newlines with spaces is not enough for shell commands | 说明产品差异，强调状态机规则和多命令边界 |
| P0 | AI Command Fixer 是什么？ | What is AI Command Fixer? | 产品介绍页，可复用到 README、Product Hunt、官网 |
| P0 | 隐私说明：你的命令不会离开浏览器 | Privacy: Your commands never leave the browser | 建立信任，解释 localStorage 只保存偏好 |

### P1 发布首月建议

| 优先级 | 中文标题 | English title | 目标 |
|---|---|---|---|
| P1 | 修复 Python `-c` 里的 SQL 折行问题 | Fix line breaks inside Python `-c` SQL commands | 覆盖真实高频场景 |
| P1 | 如何修复 AI 生成的多行部署命令 | How to fix multi-line deployment commands generated by AI | 面向 DevOps 和部署场景 |
| P1 | 反斜杠续行、JSON 参数和复制失败：三个常见命令折行坑 | Backslash continuations, JSON arguments, and broken command copies | 聚合典型问题 |
| P1 | 为什么 `<<EOF` 不应该被压成一行？ | Why `<<EOF` should not be collapsed into one line | 解释 here-doc 边界，减少误解 |
| P1 | AI Command Fixer vs ShellCheck vs explainshell | AI Command Fixer vs ShellCheck vs explainshell | 明确定位：格式修复、脚本分析、参数解释三者不同 |

### P2 长尾内容

| 优先级 | 中文标题 | English title | 目标 |
|---|---|---|---|
| P2 | 从 ChatGPT 复制命令到 SSH 终端前应该检查什么？ | What to check before pasting ChatGPT commands into SSH terminals | 安全教育与搜索长尾 |
| P2 | 本地优先开发者工具为什么更值得信任？ | Why local-first developer tools are easier to trust | 品牌信任内容 |
| P2 | 命令修复工具的产品边界：我们修格式，不替你执行判断 | The boundary of a command fixer: formatting help, not execution safety | 降低安全误解 |
| P2 | 如何收集失败样例而不泄露敏感命令？ | How to report parsing failures without leaking sensitive commands | 社区反馈指南 |
| P2 | 纯前端工具如何部署到公司内网？ | How to deploy a static developer tool inside a company network | 面向私有化和客户托管 |

## 2. FAQ 条目

### 产品基础

- AI Command Fixer 是什么？
- 它解决什么问题？
- 它适合哪些用户？
- 它和普通换行删除工具有什么区别？
- 它和 ShellCheck 有什么区别？
- 它和 explainshell 有什么区别？

### 使用方式

- 我可以粘贴整段 AI 回复吗？
- 支持一次识别多条命令吗？
- 复制全部会如何分隔多条命令？
- 支持 Python `-c` 命令吗？
- 支持 SQL 查询折行吗？
- 支持 JSON 参数吗？
- 支持反斜杠续行吗？
- 支持 `cd /path` 后接下一条命令吗？
- 没有识别出命令怎么办？
- 可以添加自定义命令前缀吗？

### 隐私与安全

- 工具会上传我的命令吗？
- 工具会保存命令历史吗？
- localStorage 里保存了什么？
- 如何清除本地设置？
- 可以离线使用吗？
- 可以部署到公司内网吗？
- 工具会检查命令是否危险吗？
- 为什么高风险命令仍然可以复制？
- 是否建议直接在生产服务器执行修复后的命令？

### 限制与路线

- 支持 `<<EOF` / here-doc 吗？
- 支持多行脚本吗？
- 支持 base64 脚本传输吗？
- 会加入浏览器插件吗？
- 会加入中英双语吗？
- 会加入命令解释功能吗？
- 会加入团队规则库吗？

## 3. 落地页文案方向

### 中文方向

主标题候选：

- 修复 AI 生成命令里的意外折行
- 把被 AI 折断的命令变回可复制的一行
- 专为 AI 命令准备的本地折行修复器

副标题候选：

- 粘贴整段 AI 回复，自动识别多条命令，修复换行和续行缩进，一键复制可执行的单行命令。
- 所有处理都在浏览器本地完成，不上传命令，不保存命令历史。
- 不是 ShellCheck，也不是命令解释器；它只专注解决 AI 回复里的命令折行问题。

卖点短句：

- 本地处理
- 不上传命令
- 不保存历史
- 多命令识别
- 复制全部
- 风险提示
- here-doc 暂不自动处理

CTA：

- 粘贴并修复
- 试用示例
- 查看 FAQ
- 在 GitHub 查看

### English direction

Headline candidates:

- Fix accidental line breaks in AI-generated commands
- Turn broken AI commands into copy-ready one-liners
- A local-first line break fixer for AI-generated terminal commands

Subheadline candidates:

- Paste an AI response, detect multiple terminal commands, repair accidental line breaks, and copy ready-to-use one-line commands.
- Runs entirely in your browser. No uploads. No command history.
- Not a shell linter, not a command explainer. Just a focused fixer for broken AI command formatting.

Benefit phrases:

- Local-first
- No uploads
- No command history
- Multi-command detection
- Copy all
- Risk hints
- here-doc not auto-collapsed

CTA:

- Paste and fix
- Try example
- Read FAQ
- View on GitHub

## 4. Product Hunt 素材

Product name:

- AI Command Fixer

Tagline options:

- Fix broken AI-generated terminal commands locally
- Turn copied AI commands into copy-ready one-liners
- Local-first fixer for accidental command line breaks

Description draft:

AI Command Fixer fixes accidental line breaks in AI-generated terminal commands. Paste a whole AI response, detect multiple commands, repair broken one-liners, and copy them back to your terminal. Everything runs locally in your browser. No uploads, no login, no command history.

Maker comment outline:

- I built this after repeatedly copying AI-generated deployment and verification commands into SSH sessions, only to hit syntax errors caused by accidental line breaks.
- Existing line break removers are too generic: replacing every newline with a space can merge separate commands or leave broken spacing inside quoted strings.
- This MVP focuses on one job: fix command formatting caused by AI/chat line wrapping.
- It runs entirely in the browser and does not upload or store command history.
- Current limits: here-doc and complex multi-line scripts are detected but not auto-collapsed.
- Feedback wanted: real examples of broken AI commands, especially Python `-c`, SQL, Docker, kubectl, and deployment scripts.

Gallery screenshot list:

- Paste AI response and detect two commands.
- Python `-c` SQL line break fixed.
- Copy all multiple commands.
- Risk hint on dangerous command.
- Privacy badges: local-first, no uploads, no history.

## 5. GitHub README 素材

README structure:

- Product name and one-line description.
- Screenshot or GIF.
- Why this exists.
- Features.
- Privacy model.
- What it does not do.
- Local usage.
- Deployment.
- Examples.
- FAQ.
- Roadmap.
- Contributing / feedback guidance.
- License.

README feature bullets:

- Paste a full AI response or a single command.
- Detect multiple ordinary terminal commands.
- Repair accidental line breaks and backslash continuations.
- Copy individual commands or copy all.
- Detect unsupported here-doc structures.
- Show risk hints for dangerous commands.
- Store preferences locally without saving command history.

Privacy section draft:

AI Command Fixer runs entirely in your browser. It does not upload command text, does not require an account, and does not save command history. Browser storage is used only for preferences such as display options and custom command prefixes.

Limitations section draft:

AI Command Fixer is a formatting repair tool. It does not execute commands, explain all command behavior, or guarantee that a command is safe. The MVP does not auto-collapse here-doc (`<<EOF`) or complex multi-line scripts.

GitHub topics:

- `ai`
- `command-line`
- `shell`
- `developer-tools`
- `static-site`
- `privacy-first`
- `local-first`
- `chatgpt`
- `terminal`
- `formatter`

## 6. 社媒发布素材

### 中文短帖

版本 A：

最近经常遇到一个很烦的问题：AI 给的服务器命令本来应该一行执行，但聊天界面把 Python `-c`、SQL、JSON 参数折成了多行，复制到终端就直接报错。

我做了一个小工具：AI Command Fixer。粘贴整段 AI 回复，自动识别多条命令，修复意外折行，然后复制回终端。

重点是：浏览器本地处理，不上传命令，不保存历史。

版本 B：

不是 ShellCheck，不解释命令，也不执行命令。

AI Command Fixer 只解决一个小但高频的问题：把 AI 回复里被意外折断的命令，修成可复制的一行。

支持多命令识别、复制全部、风险提示。本地运行，不上传命令。

### English short posts

Version A:

I kept copying AI-generated deployment commands into SSH sessions and hitting syntax errors because the chat UI wrapped a `python -c` or SQL string across lines.

So I built AI Command Fixer: paste an AI response, detect commands, repair accidental line breaks, and copy one-line commands back to your terminal.

Runs locally in your browser. No uploads. No command history.

Version B:

AI Command Fixer is not a shell linter and not a command explainer.

It does one narrow job: fix accidental line breaks in AI-generated terminal commands so they are copy-ready again.

Local-first, no uploads, multi-command detection, copy all.

## 7. 发布渠道素材差异

V2EX：

- 标题建议：`做了一个修复 AI 命令折行的小工具，想听听大家真实使用场景`
- 语气：真实问题 + 求反馈。
- 不要过度营销。

掘金：

- 标题建议：`我用状态机做了一个 AI 命令折行修复器`
- 内容重点：技术实现、状态机、隐私边界、测试样例。

Hacker News：

- 标题建议：`Show HN: AI Command Fixer - fix broken AI-generated terminal commands locally`
- 内容重点：小工具、浏览器本地、无需账号、明确限制。

Reddit：

- 标题建议：`A local-first tool to fix accidental line breaks in AI-generated shell commands`
- 内容重点：说明具体痛点，邀请真实失败样例。

Product Hunt：

- 内容重点：截图、动图、简短场景、隐私承诺。
- 发布前最好已有英文 landing、README、FAQ。

## 8. Changelog 初版模板

```markdown
# Changelog

## v0.1.0 - MVP

- Added local-first command line break fixing.
- Added multi-command detection from pasted AI responses.
- Added support for Python `-c`, SQL line breaks, JSON arguments, and backslash continuations.
- Added copy individual command and copy all.
- Added here-doc detection as unsupported.
- Added risk hints for dangerous commands.
- Added local preferences without command history storage.
```

## 9. Roadmap 初版模板

```markdown
# Roadmap

## Now

- Improve command detection accuracy with real user examples.
- Add bilingual FAQ and README.
- Add SEO metadata and structured FAQ content.

## Next

- Add English/Chinese UI language switch.
- Add import/export for custom command prefixes.
- Add browser extension exploration.
- Improve diff display for repaired commands.

## Later

- Explore here-doc to script-file mode.
- Explore team-shared rules without storing command contents by default.
```

## 10. 产品经理待确认问题

1. 第一版公开发布时，主站语言优先中文、英文，还是中英并行？
2. 项目是否准备开源？如果开源，需要确认 license、README 风格和 GitHub issue 模板。
3. 是否允许接入隐私友好的匿名统计？如果允许，需要确认只记录行为计数，不记录任何命令内容。
